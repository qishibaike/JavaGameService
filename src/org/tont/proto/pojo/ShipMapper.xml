<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.tont.proto.pojo.ShipMapper">
    
    <select id="getShipsByPid" parameterType="int"
        resultType="org.tont.proto.pojo.Ship">
        SELECT * FROM PLAYER_SHIP WHERE PID=#{pid}
    </select>
    
</mapper>